{
  "schemaVersion": "2026-03-reference-architecture-1",
  "library": {
    "id": "ffmpeg",
    "displayName": "FFmpeg",
    "displayNameArabic": "FFmpeg",
    "description": "مرجع عربي لمكتبة FFmpeg لمعالجة الصوت والفيديو."
  },
  "kind": {
    "id": "examples",
    "displayName": "Examples",
    "displayNameArabic": "الأمثلة",
    "description": "أمثلة برمجية توضح استخدام FFmpeg."
  },
  "identity": {
    "name": "vaapi_encode.c",
    "slug": "vaapi-encode",
    "titleArabic": "ترميز فيديو باستخدام VAAPI",
    "aliases": [
      "vaapi_encode",
      "vaapi_encode"
    ]
  },
  "route": {
    "hash": "#ref/ffmpeg/examples/vaapi-encode",
    "contentPath": "content/reference/ffmpeg/examples/vaapi-encode.json"
  },
  "summary": {
    "actualTranslationArabic": "ترميز فيديو باستخدام VAAPI",
    "actualMeaningArabic": "يبين encoder فيديو عتادية على Linux عبر VAAPI.",
    "purposeArabic": "يبين encoder فيديو عتادية على Linux عبر VAAPI.",
    "whyUseArabic": "يبين encoder فيديو عتادية على Linux عبر VAAPI.",
    "carriedValueArabic": "يبين encoder فيديو عتادية على Linux عبر VAAPI.",
    "sourceDescription": "يبين encoder فيديو عتادية على Linux عبر VAAPI."
  },
  "signature": {
    "raw": "// vaapi_encode.c - ترميز فيديو عبر VAAPI (GPU acceleration)\n#include <libavcodec/avcodec.h>\n#include <libavutil/hwcontext.h>\n#include <stdio.h>\n\nint main(int argc, char **argv) {\n    const char *filename = argc > 1 ? argv[1] : \"input.mp4\";\n    AVFormatContext *fmt_ctx = NULL;\nn    AVCodecContext *codec_ctx = NULL;\n    AVFrame *frame = av_frame_alloc();\n    AVPacket *pkt = av_packet_alloc();\n\n    avformat_open_input(&fmt_ctx, filename, NULL, NULL);\n    avformat_find_stream_info(fmt_ctx, NULL);\n\n    int stream_idx = av_find_best_stream(fmt_ctx, AVMEDIA_TYPE_VIDEO, -1, -1, 0, NULL);\n    const AVCodec *codec = avcodec_find_decoder(fmt_ctx->streams[stream_idx]->codecpar->codec_id);\n    codec_ctx = avcodec_alloc_context3(codec);\n    avcodec_parameters_to_context(codec_ctx, fmt_ctx->streams[stream_idx]->codecpar);\n    avcodec_open2(codec_ctx, codec, NULL);\n\n    // تفعيل VAAPI\n    AVBufferRef *hw_frames_ref = av_hwframe_ctx_alloc(codec_ctx->hw_device_ctx, 32, 0);\n\n    printf(\"VAAPI encode مفعّل: %s\\n\", codec->long_name);\n\n    while (av_read_frame(fmt_ctx, pkt) >= 0) {\n        if (pkt->stream_index == stream_idx) {\n            avcodec_send_packet(codec_ctx, pkt);\n            while (avcodec_receive_frame(codec_ctx, frame) >= 0) {\n                printf(\"frame: %dx%d\\n\", frame->width, frame->height);\n            }\n        }\n        av_packet_unref(pkt);\n    }\n\n    av_frame_free(&frame);\n    av_packet_free(&pkt);\n    avcodec_free_context(&codec_ctx);\n    avformat_close_input(&fmt_ctx);\n    return 0;\n}",
    "returnType": "",
    "parameters": [],
    "header": "",
    "headerUrl": ""
  },
  "details": {
    "parameters": [],
    "fields": [],
    "values": [],
    "usage": [
      "يبين encoder فيديو عتادية على Linux عبر VAAPI."
    ],
    "notes": [
      "يبين encoder فيديو عتادية على Linux عبر VAAPI."
    ],
    "remarks": [],
    "instructions": [],
    "lifecycle": [],
    "returns": "",
    "threadSafety": "",
    "example": "// vaapi_encode.c - ترميز فيديو عبر VAAPI (GPU acceleration)\n#include <libavcodec/avcodec.h>\n#include <libavutil/hwcontext.h>\n#include <stdio.h>\n\nint main(int argc, char **argv) {\n    const char *filename = argc > 1 ? argv[1] : \"input.mp4\";\n    AVFormatContext *fmt_ctx = NULL;\n    AVCodecContext *codec_ctx = NULL;\n    AVFrame *frame = av_frame_alloc();\n    AVPacket *pkt = av_packet_alloc();\n\n    avformat_open_input(&fmt_ctx, filename, NULL, NULL);\n    avformat_find_stream_info(fmt_ctx, NULL);\n\n    int stream_idx = av_find_best_stream(fmt_ctx, AVMEDIA_TYPE_VIDEO, -1, -1, 0, NULL);\n    const AVCodec *codec = avcodec_find_decoder(fmt_ctx->streams[stream_idx]->codecpar->codec_id);\n    codec_ctx = avcodec_alloc_context3(codec);\n    avcodec_parameters_to_context(codec_ctx, fmt_ctx->streams[stream_idx]->codecpar);\n    avcodec_open2(codec_ctx, codec, NULL);\n\n    // تفعيل VAAPI\n    AVBufferRef *hw_frames_ref = av_hwframe_ctx_alloc(codec_ctx->hw_device_ctx, 32, 0);\n\n    printf(\"VAAPI encode مفعّل: %s\n\", codec->long_name);\n\n    while (av_read_frame(fmt_ctx, pkt) >= 0) {\n        if (pkt->stream_index == stream_idx) {\n            avcodec_send_packet(codec_ctx, pkt);\n            while (avcodec_receive_frame(codec_ctx, frame) >= 0) {\n                printf(\"frame: %dx%d\n\", frame->width, frame->height);\n            }\n        }\n        av_packet_unref(pkt);\n    }\n\n    av_frame_free(&frame);\n    av_packet_free(&pkt);\n    avcodec_free_context(&codec_ctx);\n    avformat_close_input(&fmt_ctx);\n    return 0;\n}",
    "examples": [
      {
        "title": "ترميز فيديو باستخدام VAAPI",
        "code": "// vaapi_encode.c - ترميز فيديو عبر VAAPI (GPU acceleration)\n#include <libavcodec/avcodec.h>\n#include <libavutil/hwcontext.h>\n#include <stdio.h>\n\nint main(int argc, char **argv) {\n    const char *filename = argc > 1 ? argv[1] : \"input.mp4\";\n    AVFormatContext *fmt_ctx = NULL;\n    AVCodecContext *codec_ctx = NULL;\n    AVFrame *frame = av_frame_alloc();\n    AVPacket *pkt = av_packet_alloc();\n\n    avformat_open_input(&fmt_ctx, filename, NULL, NULL);\n    avformat_find_stream_info(fmt_ctx, NULL);\n\n    int stream_idx = av_find_best_stream(fmt_ctx, AVMEDIA_TYPE_VIDEO, -1, -1, 0, NULL);\n    const AVCodec *codec = avcodec_find_decoder(fmt_ctx->streams[stream_idx]->codecpar->codec_id);\n    codec_ctx = avcodec_alloc_context3(codec);\n    avcodec_parameters_to_context(codec_ctx, fmt_ctx->streams[stream_idx]->codecpar);\n    avcodec_open2(codec_ctx, codec, NULL);\n\n    // تفعيل VAAPI\n    AVBufferRef *hw_frames_ref = av_hwframe_ctx_alloc(codec_ctx->hw_device_ctx, 32, 0);\n\n    printf(\"VAAPI encode مفعّل: %s\n\", codec->long_name);\n\n    while (av_read_frame(fmt_ctx, pkt) >= 0) {\n        if (pkt->stream_index == stream_idx) {\n            avcodec_send_packet(codec_ctx, pkt);\n            while (avcodec_receive_frame(codec_ctx, frame) >= 0) {\n                printf(\"frame: %dx%d\n\", frame->width, frame->height);\n            }\n        }\n        av_packet_unref(pkt);\n    }\n\n    av_frame_free(&frame);\n    av_packet_free(&pkt);\n    avcodec_free_context(&codec_ctx);\n    avformat_close_input(&fmt_ctx);\n    return 0;\n}"
      }
    ]
  },
  "links": {
    "officialUrl": "https://www.ffmpeg.org/doxygen/8.0/vaapi_encode_8c-example.html",
    "sourceUrl": "",
    "related": []
  },
  "source": {
    "dataset": "data/ui/ffmpeg/search.json",
    "category": "التسريع العتادي",
    "packageKey": "ffmpeg",
    "packageName": "FFmpeg"
  }
}